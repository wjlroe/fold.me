<section id="main">

  <div class="container">
    <div class="row">
      <% folds.forEach(function(fold) { %>
        <div class="threecol">
          <img src="<%= fold %>">
        </div>
      <% }) %>
    </div>
  </div>

</section>
