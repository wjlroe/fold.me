google_images = require('./lib/google-images')
Set = require('Set')

print_images = (images) ->
  urls = (image.url for image in images)
  console.log(urls)
  url_set = new Set(urls)
  if urls.length > url_set.size()
    console.log("bad")

google_images.searchPages('scotch fold', 5, print_images)
